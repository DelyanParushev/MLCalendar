# Vercel-specific requirements (without heavy ML libraries)
# The ML inference is handled by HuggingFace Space, so we don't need torch/transformers

fastapi>=0.110
uvicorn[standard]>=0.23
SQLAlchemy>=2.0
pydantic>=2.5
pydantic[email]>=2.5
python-dotenv>=1.0
dateparser>=1.2
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
bcrypt==4.0.1
python-multipart>=0.0.6
psycopg2-binary>=2.9.0
google-auth>=2.0.0
google-auth-oauthlib>=1.0.0
google-auth-httplib2>=0.2.0
mangum>=0.17.0

# Note: torch and transformers are NOT included for Vercel deployment
# ML inference is handled by HuggingFace Space API
